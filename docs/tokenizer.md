# Tokenizer

The tokenizer prepares text for transformer training by converting raw text into token IDs.

The project includes:

- Vocabulary creation
- Token encoding
- Sequence preparation
- Padding
- Batching
